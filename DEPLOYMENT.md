# 局域网离线供应商培训平台 - 部署指南

## 一、平台概述

本平台是一套基于React + Vite + Tailwind CSS构建的静态离线网页站点，无需外网服务器，全部资源本地存储，同一局域网内任意设备均可访问。

## 二、完整搭建流程

### 阶段1：环境准备

1. **安装Node.js**（版本18+）
   - 下载地址：https://nodejs.org/
   - 安装完成后验证：`node -v` 和 `npm -v`

2. **克隆/下载项目**
   - 将项目文件复制到本地主机

### 阶段2：项目构建

1. **进入项目目录**
   ```bash
   cd /path/to/project
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **构建生产版本**
   ```bash
   npm run build
   ```
   - 构建完成后，所有资源会输出到 `dist/` 目录

### 阶段3：局域网部署

#### 方法一：一键启动局域网服务器（强烈推荐）

这是最简单、最稳定的局域网共享方式，自动识别本机所有IP地址。

1. **启动服务**
   ```bash
   # 方式1：只启动服务器（需先构建）
   npm run server

   # 方式2：构建+启动一站式完成
   npm run deploy
   ```

2. **查看访问地址**
   启动后终端会自动显示所有可用地址：
   ```
   ========================================
   局域网离线供应商培训平台已启动
   ========================================

   本地访问地址:
     http://localhost:8080
     http://127.0.0.1:8080

   局域网访问地址:
     http://192.168.1.100:8080
     http://192.168.1.101:8080

   请在同一局域网内的设备浏览器中输入以上地址访问平台
   ========================================

   按 Ctrl+C 停止服务
   ```

3. **局域网内其他设备访问**
   - 电脑：在浏览器地址栏输入局域网地址（如 `http://192.168.1.100:8080`）
   - 平板/手机：连接同一WiFi，在浏览器中输入局域网地址

#### 方法二：本地文件共享

1. **复制dist目录**
   - 将构建生成的 `dist/` 文件夹复制到局域网共享主机的任意位置

2. **开启文件共享**
   - 在Windows上：右键 `dist` 文件夹 → 属性 → 共享 → 高级共享
   - 在Linux上：使用 `samba` 或 `nfs` 配置共享目录

3. **访问平台**
   - 局域网内其他设备通过访问共享路径打开 `index.html`
   - 示例路径：`\\主机IP\共享名\dist\index.html`

#### 方法三：简易HTTP服务（备用）

1. **启动本地服务器**
   ```bash
   # 方法1：使用Python（Python 3）
   cd dist
   python -m http.server 8080

   # 方法2：使用Node.js
   cd dist
   npx http-server -p 8080

   # 方法3：使用Vite预览
   npm run preview -- --port 8080
   ```

2. **访问平台**
   - 在同一局域网内的设备浏览器中访问：`http://主机IP:8080`

#### 方法四：U盘/移动硬盘分发

1. **复制dist目录**
   - 将 `dist/` 文件夹完整复制到U盘或移动硬盘

2. **离线访问**
   - 将U盘插入任意设备
   - 直接双击 `dist/index.html` 文件打开
   - 无需网络，全部内容离线浏览

## 三、页面层级结构

```
├── 首页（合作招商首页）
│   ├── 核心优势展示
│   ├── 四大项目入口按钮
│   ├── 合作流程图
│   └── 联系方式
├── 四大项目专区
│   ├── AT头戴采集专区
│   ├── DF iPhone采集专区
│   ├── BTS流水线专项专区
│   └── SW头戴设备专区
├── SOP运维资料库
├── 质量判定中心
├── 异常故障处理手册
├── 离线表单工具区
└── 物流邮寄&结算说明
```

## 四、离线访问特性

### 无需外网
- 所有资源（HTML、CSS、JS、图片）均本地存储
- 无任何CDN依赖或在线API调用
- 使用系统字体，无需在线加载

### 离线表单功能
- 表单数据保存在浏览器 localStorage
- 支持CSV导出，可导入Excel查看
- 数据持久化，关闭页面后重新打开仍可查看

### 跨设备访问
- 支持电脑、平板、手机访问
- 响应式设计，自动适配不同屏幕尺寸
- 触摸友好的按钮和交互

## 五、平台内容说明

### 素材来源
- AT&DF设备运维SOP.docx
- BTS项目.docx
- DF-iPhone 5月29日SOP.docx
- SW采集sop.docx

### 核心模块
1. **合作招商首页**：展示合作优势、项目入口、联系方式
2. **四大项目专区**：AT/DF/BTS/SW设备独立培训内容
3. **SOP运维库**：设备操作规范文档，支持搜索和打印
4. **质量判定中心**：废片分级标准、实拍案例对照
5. **故障速查手册**：按设备分类的故障处理方案
6. **离线表单**：报备模板、设备签收、工序申报等
7. **物流结算**：邮寄信息、结算规则、对接人分工

## 六、更新维护

### 更新内容
1. 修改 `src/data/content.ts` 中的数据
2. 运行 `npm run build` 重新构建
3. 将新的 `dist/` 文件夹替换旧版本

### 更新图片
1. 将新图片放入 `src/assets/images/`
2. 在对应页面组件中导入并使用
3. 运行 `npm run build` 重新构建

## 七、技术栈

- **框架**：React 18 + TypeScript
- **构建工具**：Vite 6
- **样式**：Tailwind CSS 3
- **状态管理**：React useState
- **数据存储**：localStorage（离线表单）

## 八、注意事项

1. **网络要求**：无需外网，但BTS项目采集时需要工厂WiFi
2. **设备要求**：支持现代浏览器（Chrome、Edge、Safari、Firefox）
3. **安全提示**：表单数据保存在浏览器本地，清除浏览器数据会丢失表单记录
4. **备份建议**：定期导出CSV文件备份表单数据

## 九、Windows防火墙配置（重要）

如果局域网内其他设备无法访问平台，请按以下步骤配置防火墙：

### 方法一：自动配置（管理员权限）

以管理员身份打开命令提示符，执行以下命令：
```cmd
netsh advfirewall firewall add rule name="局域网供应商平台" dir=in action=allow protocol=TCP localport=8080-8084
```

### 方法二：手动配置

1. 打开「控制面板」→「Windows Defender 防火墙」→「高级设置」
2. 在左侧点击「入站规则」→「新建规则」
3. 选择「端口」→ 下一步
4. 选择「TCP」→ 填写「特定本地端口」：`8080-8084` → 下一步
5. 选择「允许连接」→ 下一步
6. 保持默认设置 → 下一步
7. 名称填写「局域网供应商平台」→ 完成

### 方法三：临时关闭防火墙（不推荐）

仅用于测试，测试完成后请恢复防火墙设置：
```cmd
netsh advfirewall set allprofiles state off
```

## 十、快速启动方式

### 方式一：双击启动（推荐）

直接双击项目根目录下的 `start.bat` 文件，自动完成：
1. 检查并安装依赖（首次运行）
2. 构建项目
3. 启动局域网服务器

### 方式二：命令行启动

```bash
# 开发模式（调试用）
npm run dev

# 生产模式（局域网分享用）
npm run server

# 构建+启动一站式
npm run deploy
```

## 十一、故障排查

### 其他设备无法访问
- ✅ 确保所有设备连接同一WiFi/局域网
- ✅ 检查Windows防火墙是否允许8080-8084端口入站连接
- ✅ 确认服务器输出的局域网IP地址正确
- ✅ 尝试关闭杀毒软件或安全管家的网络防护

### 服务器启动失败
- ✅ 检查端口8080-8084是否被其他程序占用
- ✅ 确认已安装Node.js（版本18+）
- ✅ 尝试以管理员身份运行 `start.bat`

### 页面加载异常
- ✅ 确认已执行 `npm run build` 构建项目
- ✅ 检查 `dist/` 目录是否存在且包含所有文件
- ✅ 清除浏览器缓存后重新加载
